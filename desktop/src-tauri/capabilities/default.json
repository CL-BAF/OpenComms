{
  "$schema": "..\\gen\\schemas\\capabilities.json",
  "identifier": "default-capabilities",
  "description": "Deny-by-default IPC for the OpenComms Tauri Desktop GUI: the ONLY grant is spawning the bundled OpenComms coordinator sidecar with fixed argument validators (gui --port N --server --project <dir>). No broad shell, filesystem, or network access.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "binaries/opencomms-coordinator",
          "sidecar": true,
          "args": [
            "gui",
            "--port",
            { "validator": "\\d{1,5}" },
            "--server",
            "--project",
            { "validator": "\\S+" }
          ]
        }
      ]
    }
  ]
}